// import { App } from "vue";
// import { locale } from "./i18n";
// interface Options {
//   locale: string;
// }
// const defaultInstallOpt = {
//   locale: "zh-cn"
// };

export default {
  // eslint-disable-next-line @typescript-eslint/explicit-module-boundary-types
  // install(app: App, option: Options) {
  //   // const mergeOption = Object.assign(defaultInstallOpt, option);
  //   // locale(mergeOption.locale);
  // }
};
